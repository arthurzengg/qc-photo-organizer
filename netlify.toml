# Netlify config for a zero-build static site (no build step; serve files as-is).
[build]
  publish = "."
  command = ""

# Serverless functions (e.g. delete-record: server-side OSS delete with the
# delete key kept as an env secret, never shipped to the browser).
[functions]
  directory = "netlify/functions"

# Long-cache the vendored library (its contents change only on a version bump).
[[headers]]
  for = "/lib/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Baseline security headers for everything else.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
